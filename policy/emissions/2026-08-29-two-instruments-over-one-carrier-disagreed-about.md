<!-- tsurezure-client-kit:emission (staging candidate — the hub's gate is the sole promotion path) -->
date: 2026-08-29
repo: Kogaki
grain: lesson

## Trigger — what happened

Two instruments over one carrier disagreed about how many times it had been worked: a vitality read reported 1 PR-space touch naming the merged PR, while the run-once gate on the same issue reported 0 advancing passes and returned allowed. Both were reading the same completed sitting.

## The learning

When two gates over one carrier count the same history differently, the disagreement is invisible because each is consulted alone and each answers confidently in its own vocabulary — 'runs' and 'advancing passes' sound like the same quantity and are computed from different evidence. The gate that returns the permissive answer is the one that gets acted on, so the looser predicate silently becomes the effective rule no matter which was intended to bind. The diagnostic is cheap and almost never run: read both instruments in the same act and compare, rather than consulting whichever one the procedure names at that step. And the deeper repair is not to tighten the loose one — it is to notice that a rule stated in one vocabulary and enforced in another has no single place where its meaning lives, so 'the same carrier, worked twice' has to be defined once and derived by both, or the two definitions will keep drifting in the direction that permits more.

---

Emitted under `specs/spec-client-kit/SPEC.md` §4. This is a **candidate**:
nothing here is promoted, and nothing here writes any recall surface. The
hub's own selection gate decides whether it becomes anything.
