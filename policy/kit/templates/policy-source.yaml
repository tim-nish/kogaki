# Installed by the tsurezure client kit.
policy_source:
  enabled: true   # presence toggle only — no path; the gateway owns the hub
                  # location. Unreachable gateway: one logged degradation
                  # line, then proceed without policy interaction.
