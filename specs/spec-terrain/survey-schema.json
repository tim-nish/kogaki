{
  "note": [
    "The Terrain survey record, machine-readable (SPEC.md beside this file",
    "is the prose half; kogaki#14 umbrella, kogaki#17 story 1.8, manifest",
    "item 1). checks/check-terrain-composition.sh READS these lists rather",
    "than restating them — this file is the single carrier, so amending the",
    "contract is one edit and never a two-copy divergence. Same arrangement",
    "as record-schema.json (item 3) and gate-schema.json (item 4).",
    "The three contracts bound here are SPEC.md §2.1–§2.3: completeness is",
    "a cover counted in placements, grouping is presentation-only, and the",
    "second-proposer boundary keeps narrowing out of navigation state.",
    "v2 (kogaki#26/#27, story 1.22) carries SPEC.md §5's candidate model and",
    "§9's family-named figures: candidate rows are LESSONS ONLY, the Journey",
    "family is a per-Lesson MARK read by absence, `journeys` carries the",
    "served Journey set so count-in stays computable against count-out, and",
    "`section_required` gains `by_family` — the fill of",
    "`deferred-slot: terrain-family-split-carrier` with alternative (a), the",
    "per-section family split living in the RECORD (owner decision",
    "2026-08-06, SPEC.md §9).",
    "The single-carrier clause covers these FIELD LISTS. It does NOT cover",
    "the recompute ALGORITHM, which is written twice on purpose —",
    "terrain/terrain.mjs (generation-time refusal) and",
    "check-terrain-composition.sh (merge-layer detection). SPEC.md §9",
    "records that correction; collapsing the duplication is not licensed."
  ],
  "version": "2",
  "families": ["lesson", "journey"],
  "family_label": "Strand (Lesson|Journey)",
  "candidate_family_must_be": "lesson",
  "candidate_family_rationale": "a candidate row is ONE Lesson and a Journey is a MARK on its Lesson's row, never a row of its own (SPEC.md §5); a journey-family candidate is the two-family row model arriving back as data",
  "survey": {
    "required": ["id", "generated_by", "pin", "candidates", "journeys", "sections", "no_relation_section", "completeness"],
    "candidate_required": ["id", "family", "tags", "cite"],
    "section_required": ["name", "members", "by_family"],
    "narrowing_keys_forbidden": ["selected", "trimmed", "hidden", "excluded", "dropped"],
    "narrowing_rationale": "sections gate nothing and a navigation step carries no selection authority (SPEC.md §2.2); a narrowing key in navigation state is the refused minimal-form bundling arriving as a field",
    "journey_mark_key": "journey",
    "journey_mark_rationale": "the mark reads by ABSENCE — a Lesson with no Journey is decorated, a Lesson with one is not (SPEC.md §5); the key is present on every candidate, null when the Lesson is thin",
    "journeys_rationale": "the served Journey set, carried so count-in stays computable against count-out: Lessons plus marks reconstructs the Strand set exactly, and a Journey whose slug matches no Lesson has no row to be marked on (SPEC.md §5.2, falsifier 1)",
    "orphan_journey_rationale": "falsifier 1 — a Journey whose slug matches no Lesson candidate is silently dropped by the re-projection, so the survey is REFUSED at generation time and the orphan slugs are named; §2.1 forbids the silent drop and a rendering-time warning is not the carrier",
    "section": {
      "by_family_rationale": "the per-section family split lives in the RECORD (deferred-slot terrain-family-split-carrier, alternative (a), owner decision 2026-08-06, SPEC.md §9): placements are authoritative, the stored section figure is subordinate, and FIGURE_MISMATCH is the mechanical check — the same shape completeness.by_family already has, at the layer the figure is created",
      "counted": "lesson = member Lessons; journey = member Lessons carrying a Journey mark. The rendered section figure is their sum, which is the Strand count `agents (115 — 59 lessons + 56 journeys)` §9 names, never a bare 115"
    },
    "completeness": {
      "required": ["placed", "of", "family", "by_family", "counted_over"],
      "counted_over_must_be": "placements",
      "counted_over_rationale": "the count runs AFTER composition; a figure computed over the candidate set reads as a pass while material is missing (SPEC.md §2.1)",
      "family_must_name": "Strand (Lesson|Journey)",
      "family_rationale": "every figure names which family it counted — a bare count is a defect, not a terse rendering (SPEC.md §2.1; GLOSSARY.md:248@924cce3)",
      "denominator_key": "of",
      "denominator_rationale": "`placed` and `of` are counted IN LESSONS — every screen showing candidate rows states its denominator in Lessons (SPEC.md §5); the load-bearing half of the design is the denominator rather than the mark, because it is what makes the next coverage inversion visible on-screen. `by_family` beside it carries the Strand split, so the figure names its families without the denominator drifting off Lessons",
      "coverage_keys": ["thin_lessons", "coverage"],
      "coverage_rationale": "the coverage figure (thin Lessons / Lessons) rides beside the mark; falsifier 2's reversal trigger is coverage >= 99%, and SPEC.md §5.2 declares `instrument: none` for it — it is printed, never read by a check, and this schema does not supply the carrier that section deliberately withholds"
    }
  },
  "group_claim": {
    "note": "SPEC.md §7 — a claim composed over a member set is PINNED to that set. The record carries the member IDS, not only the claim text, because a derived expression's truth is relative to the set it was derived from.",
    "required": ["id", "kind", "pin", "group", "claim", "members", "member_pins", "composed_over", "adopted"],
    "kind_must_be": "group-claim",
    "composed_over_must_be": "members",
    "composed_over_rationale": "the claim is composed over the MEMBERS, so a change to the member set is a GATE EVENT rather than a refresh (SPEC.md §7)",
    "member_pin_required": ["id", "cite"],
    "adopted_must_be": false,
    "adopted_rationale": "a group-claim record is a per-invocation RENDERING and is never itself the adopted claim; adoption is the owner's act at the gate and lands in an adopted-claim record",
    "narrowing_keys_forbidden": ["selected", "trimmed", "hidden", "excluded", "dropped"],
    "narrowing_rationale": "composing a claim over a group is PRESENTATION and narrows nothing on its own; a subset selection is the OWNER's act, and rank/trim/hide still route through the proposal contract (SPEC.md §2.3, §8.2)"
  },
  "adopted_claim": {
    "note": "SPEC.md §7 — the brief records the adopted claim TOGETHER WITH the members it was composed from, by member id and pin, never by a group id.",
    "required": ["id", "kind", "pin", "claim", "claim_source", "members", "member_pins", "gate"],
    "kind_must_be": "adopted-claim",
    "claim_sources": ["recomposed", "original-wording-kept", "free-text"],
    "claim_source_rationale": "the recomposed claim is a PROPOSAL: the owner may keep the original wording, and the recorded member set is what makes the resulting mismatch LEGIBLE rather than forbidden (SPEC.md §7)",
    "member_pin_required": ["id", "cite"],
    "group_id_keys_forbidden": ["group", "group_id", "group_name", "cotag"],
    "group_id_rationale": "recorded by member id and pin and NEVER by a group id — a group id lets the member set change underneath the claim without the record moving, which is the provenance lie §7 refuses",
    "gate_required": ["gate_id", "stop_id", "answer"],
    "gate_rationale": "adoption happens only at the gate carrier (manifest item 4): the recomposed claim is re-offered as a GATE EVENT, never a silent refresh, so an adopted claim with no gate row was never offered"
  },
  "records_home": {
    "glob": "*.terrain-survey.json",
    "scope": "anywhere in the repository",
    "rationale": "a default carrier, not an enumerated directory — an enumerated home leaves record N+1 uncovered by default; real runs live in the machine-local run workspace and are never committed (specs/SPEC.md §4 rider 3)"
  }
}
