---
id: reg-0116
status: pending
observed_at_pr: 504
observed_at_head: 8906f20752e27d1935c62f24c8ba41ea1d55dba0
class:
recorded: 2026-08-18
source_comment: 5324383959
---
Two rows from PR #504's review-lane report (head `da15301`), appended per §4 clause 8's `carried: register`.

**Row class: accretion.** Both are the count-valuable kind — a mechanical conformance observation whose value is the recurrence rather than the instance. Neither is a spent-bound latent in-diff carry (kogaki#374, instance-class), and neither is an `out-of-dimension:` line, so **neither counts toward rule 3's three-of-a-class widening trigger**, which reads over `out-of-dimension:` lines only.

1. **A v1-shaped receipt beside v2 ones in the same section.** `specs/spec-client-kit/SPEC.md` §4.7 carries `consulted: product-lab@8906f20752e27d1935c62f24c8ba41ea1d55dba0 topics/knowledge-architecture.md:60` with no `request_id:`, `outcome:` or `query:`, while the two receipts above it carry all three. `specs/SPEC.md` §4: "a v2 receipt still owes `request_id`, `outcome` and one `query:` line". Pin resolved on the served surface at this review and the quote is verbatim, so this is form only. Recorded here because the interesting fact is how often a hand-carried quotation lands in receipt shape, not this one instance.

2. **A measurement baked into a spec that does not reproduce at its own head.** §4.7 states "125 emissions"; `ls policy/emissions | wc -l` at `da15301` returns 114, the gap being uncommitted files the PR body discloses. `policy/consultation-map.md` already carries the remedy in its own voice — name the population rather than give a bare count. Recorded here because the count-in-a-durable-carrier shape recurs across surfaces.

— PR #504 round 1
