---
id: reg-0108
status: pending
observed_at_pr:
observed_at_head:
class:
recorded: 2026-08-16
source_comment: 5307945866
---
Observations from PRs #483/#484 (kogaki#482, merged 12023e6 + 7b11cdf), each
`carried: register` by the reviewer:

From #483: (1) branch consult receipts predominantly v1-form — the `outcome`
token is the entire input of the map's miss-harvesting proposer, so v1
consults are invisible to the machine that grows the map (1 of 4 receipts
conformed at the fix head; direction of travel right). (2) The reviewer's
fixed opening tier-1 `gloss_index` survey exceeded the tool read budget for
the third consecutive round (76,961 chars, one line) — an instrument gap in
the lane's own opening move.

From #484: (3) `--briefs-dir` is the one flag outside argString()'s refusal
shape — a bare flag silently defaults instead of refusing; harm bounded (a
test seam, absent from the skill's command line), asymmetry undeclared.
(4) check-brief-entry.sh:40's regex literal terminates at the `/` in
`gloss/` — compiles one character short of the `new RegExp` neighbour it was
modelled on; discriminates anyway; one-token remedy.
