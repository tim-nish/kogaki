---
id: reg-0102
status: pending
observed_at_pr: 477
observed_at_head:
class:
recorded: 2026-08-16
source_comment: 5306564600
---
Two observations from PR #477 round 2 (story 1.69, merged ac496d3), each
`carried: register` by the reviewer — accretion-class, recorded for the count:

1. **The no-material branch drops the `unmapped` disclosure.** `neighborhoodSection`'s
   `no_material` return never reaches the `unmapped` render, though `unmapped` is a
   seam-independent local join — so a pull whose entered ids reach settled members
   naming no candidate reports that on every path except the one whose own line
   cites §13.4's disclosure discipline. Remedy known and one clause wide (render the
   block inside the branch); latent until a no-material pull has unmapped ids.

2. **`neighborhood_disjointness`'s note is falsified by v7 in the same file:** it
   records the line as "pushed UNCONDITIONALLY … renders on every section, empty
   included", and the v7 no-material branch returns before it. One-clause remedy
   (every *enumerating* section). §14.1 makes the artifact win against prose, so a
   self-contradicting note there misleads with authority.

Also standing from PR #475 round 2: five of the v6/v7 class notes cite a source
line one-off from the `say()` they describe (`:3379/:3387/:3406/:3417/:3419`);
nothing executable reads a `read off` note.
