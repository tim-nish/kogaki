---
id: reg-0038
status: pending
observed_at_pr: 363
observed_at_head:
class: out-of-dimension
recorded: 2026-08-11
source_comment: 5254250491
---
out-of-dimension: the lane's fixed opening move — the unscoped tier-1 `gloss_index` survey — is unperformable from inside a review session. The result is ~77 000 characters on a single line: it exceeds the tool-result limit, is written to a file, and that file's one line cannot be chunked by `Read`'s offset/limit, while the byte-slicing shapes that could chunk it (`python3`, `cut -c`) are denied by design. Second consecutive occurrence — PR #363 (rounds 1 and 2) and now PR #364 round 1, where it was recorded as a `cannot-determine:` line in each report. Same class each time: the lane prescribes a first move whose instrument returns a payload the lane's own grants cannot read.
