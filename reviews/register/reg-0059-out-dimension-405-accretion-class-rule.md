---
id: reg-0059
status: pending
observed_at_pr: 405
observed_at_head:
class: out-of-dimension
recorded: 2026-08-12
source_comment: 5270141500
---
out-of-dimension: PR #405 — **accretion-class** (rule 3's three-of-a-class trigger reads over these rows; this is not a spent-bound carry).

The lane's **fixed first move** — an unscoped tier-1 `gloss_index` survey — is **unreadable from inside a review with the granted tool set**. The unscoped call returned **76,961 characters on a single line**; the harness spilled it to a file, `Read` cannot chunk a one-line file of that size, `Grep` returns the line whole, and the fallback the spill message itself suggests (`python3` slicing) is denied by composition constraint 3.

So the opening move the lane declares as fixed is, today, structurally unperformable — every reviewer either spends turns discovering this or skips the survey silently. This review recorded it as `cannot-determine: the opening tier-1 gloss_index survey` and proceeded on the two dimensions.

Instrument shape that would close it (named, not filed): a bounded read of the tier-1 index — a paging argument, a headline-only projection, or a spill format the granted readers can chunk. Per the lane's own rule, a reviewer that finds itself needing a parser has found a gap in the sweep's instruments, not a task for the turn.
