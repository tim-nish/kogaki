---
id: reg-0096
status: pending
observed_at_pr: 475
observed_at_head: f72f70a
class:
recorded: 2026-08-16
source_comment: 5306302694
---
Appended by the review lane from PR #475 (head `f72f70a`). Two rows, each labelled by class per kogaki#374, since this ledger has two producers and rule 3's three-of-a-class trigger reads `out-of-dimension:` rows ONLY.

**ACCRETION-CLASS — `out-of-dimension:`** (counts toward rule 3)

out-of-dimension: CLAUDE.md's emission duty says a sitting producing a durable learning writes ONE staging candidate in the same sitting, unasked. The 2026-08-16 sitting behind PR #475 produced at least two — the correction of kogaki#472's own reachability instrument (the declared `grep` hit one site, not two, and its empty-query condition would have read clean with the spec untouched), and the read that `line_class_allowlist`'s inertness is a property of the SURFACE rather than of its three original classes. `policy/emissions/` at that head carries nothing dated later than 2026-08-12. Neither review dimension types this, and no registered check observes it. (PR #475)

**ACCRETION-CLASS — a `carried: register` disposition on a non-gating finding** (kogaki#224 / SPEC §4 clause 8). Recorded for its count, not its instance:

Stale shipped-code pointers in `specs/spec-terrain/SPEC.md`, three of four sitting in the two clauses PR #475's amendment argues from, while the pointers the diff ADDS are correct: §14.6 says `NEIGHBOR_ID` at `terrain/terrain.mjs:2728` where v6's grammar correctly says `:2828`; §14.1's six-surface enumeration says `cmdView :447` / `cmdCotags :574` where the definitions are `:558` / `:723`, and §13.1's new text correctly cites the `writeScreen` call sites `:618` / `:981`. Pre-existing, introduced by no diff under review, and non-gating — the count is what would justify an instrument (nothing checks a spec's code pointers), not any one instance.
