---
id: reg-0089
status: pending
observed_at_pr: 463
observed_at_head:
class:
recorded: 2026-08-14
source_comment: 5296832108
---
Rows from PR #463 (merged `ac28865`), rounds 1 and 2.

out-of-dimension: [accretion-class] PR #463 rounds 1 and 2 — a reachability instrument whose count is line-wrap dependent. kogaki#462's floor names `grep -n 'in full, in the user-visible reply' specs/spec-terrain/SPEC.md`, and the reported count is correct as run, but both marked quotations of the superseded wording evade the line-oriented grep only because they wrap across lines. A re-flow of either paragraph would move the floor's number without moving the file's meaning — and round 2 observed that this PR's own fix commit re-flowed one of those paragraphs without moving it, which is the sensitivity demonstrated rather than refuted. The substantive claim (both live clause sites read zero) is unaffected. Same instance across both rounds, counted once.

**Round 2's finding 4 is NOT registered here, and that is the disagreement worth recording.** The round carried it to the register while stating in its own `cannot-determine` line that the register was a **fallback** rather than the right home — `story-sync file-issue` was not granted in that session, so it could not mint the carrier it judged was owed. Filed instead as **kogaki#464**, on the round's own reasoning, plus one fact the round could not see: the story it named as the bounding carrier (1.66) is bound to **#434, which is closed**, so the interval it treated as bounded had nothing scheduled to end it. A round that names its carrier a fallback is asking for the real one.
