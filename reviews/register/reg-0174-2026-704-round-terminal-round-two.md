---
id: reg-0174
status: pending
observed_at_pr: 704
observed_at_head:
class:
recorded: 2026-08-29
source_comment: 5461714714
---
**2026-08-29 — PR #704 round 2 (terminal round), two nits carried rather than fixed.**

The two-round bound is spent; a head move now would owe a report no round can
cover. Both are non-gating and neither is a defect in what merged.

**1. `owner_surface_coverage` is a mechanically evaluable rule with a
hand-written answer beside it, and nothing evaluates one against the other.**
`specs/spec-terrain/workflow.json`. PR #704 round 1 caught the field reading
`4 of 4` after v9 halved the denominator its own sibling `rule` defines; the
repair fixed the **value** and left the **class** open. The field's own
`recomputed_never_transcribed` text now records the drift, and this sitting's
emission names why the disclaimer did not protect it — *"a convenience for a
human reader and carries no authority"* reads as already-handled, so the next
editor skips it, and it drifts precisely in the edits that change the quantity
it reports.

The carrier would be a registered check evaluating the rule over the states
array and comparing. That is **outside what kogaki#682 authorizes** — its three
dispositions all concern the Screen definition — and a new check owes an
admission record with a removal signal declared at birth, which is its own
decision act. Filed here rather than minted.

**What would make it actionable:** a second field in this family found stale by
the same shape (a stated derivation with a transcribed answer and no evaluator).
One instance is a repaired value; two is a class with a carrier.

**2. Two round-1 edits are spliced into wrapped prose without re-wrapping.**
`specs/spec-terrain/SPEC.md:5599` and `:5636-5638` run well past the column the
rest of the file holds. Cosmetic only — it makes the amendments visible as
splices in an otherwise uniformly wrapped file. Carried because the bound is
spent and a cosmetic head move is not worth the spent-bound exit.

carried from: PR #704 round 2 · licensing issue kogaki#682
