---
id: reg-0099
status: pending
observed_at_pr: 476
observed_at_head: e7ce0f1
class:
recorded: 2026-08-16
source_comment: 5306512209
---
## Append from PR #476 (head `e7ce0f1`) — two rows, both **accretion-class**

Both are `out-of-dimension:` rows: their value is the **count**, not the instance.
Neither is a spent-bound latent in-diff carry, so both are countable toward rule 3's
three-of-a-class widening trigger.

`out-of-dimension:` PR #476 — the emission duty (`CLAUDE.md`, *"A sitting that
produces a durable learning … writes ONE staging-candidate emission in the same
sitting, unasked"*) appears undischarged across recent spec sittings:
`policy/emissions/`'s newest file is dated **2026-08-12**, and kogaki#469, #468,
#471, #472 and #474 have all sat since. The check that runs today asserts the
directory is committed and the writer is well-formed — nothing observes whether a
sitting that produced a learning emitted one, which is the obligation half.

`out-of-dimension:` PR #476 — a spec amendment landing without an entry in the
amended file's own Status/amendment register, now twice consecutively:
`e9c509d` (kogaki#472) amended `specs/spec-terrain/SPEC.md` §§13.1–13.4, 13.7 and
14.4.1 with no Status entry, and PR #476 repeats it on **two** files
(`spec-terrain` §14.4.1→v20, `spec-draft-pipeline` §6.9.2→v2). The instance is
reported as finding 3 on #476 and resolves in that review; this row records the
recurrence, and the second-order effect the count makes visible: with the register
unmaintained, `spec-terrain`'s version tokens now map v19→both kogaki#462 and
kogaki#472, and v20→both kogaki#472 and kogaki#474.

— posted by the review lane, per SKILL rule 1.
