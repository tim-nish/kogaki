---
id: reg-0029
status: pending
observed_at_pr: 337
observed_at_head: ec88ea4
class:
recorded: 2026-08-09
source_comment: 5232480703
---
**Register append — review lane, PR #337 round 2 (head `ec88ea4`).**

out-of-dimension: PR #337 — the lane's declared **fixed first move** — an UNSCOPED
tier-1 `gloss_index` survey, chosen precisely so that *where to look* is an output
of the survey rather than a heading the reviewer supplies — is **unperformable in
this harness**. The no-tag call returns 76,961 characters on a single line and is
refused as over-limit; the harness's own fallback advice is to byte-slice the saved
transcript, which `.claude/skills/review-lane/SKILL.md` puts out of scope for a
per-PR review ("a reviewer that finds itself needing a parser has found a gap in
the sweep's own instruments").

**Two rounds in a row, same wall.** PR #337's fix commit `ec88ea4` records it from
the authoring side in the same words — "the unscoped read exceeds the harness
output limit" — and this round hit it from the reviewing side. Both sittings worked
around it by reaching for a **scoped** shard, which is exactly the substitution the
fixed first move exists to refuse: a scoped query can only return lines about
something you already thought to name.

**Class: mechanical**, so per rule 4 this escalates immediately rather than waiting
for three of a class. The remedy is an instrument — a paged or headline-only tier-1
form, or a documented bounded first move that preserves the unscoped property —
held wherever the seam's tool surface is owned, not a reviewer improvising a
workaround each round. Recorded here rather than acted on: no sitting of this lane
may add or amend a dimension, and this is the register doing its one job.
