---
id: reg-0058
status: pending
observed_at_pr: 399
observed_at_head: c2d4f34
class:
recorded: 2026-08-12
source_comment: 5268408709
---
Append from the review lane — PR #399 (`spec(SPEC §4 clause 12)`, kogaki#269), head `c2d4f34`.

**Row kinds are declared, per kogaki#374:** one accretion-class `out-of-dimension:` row, and four instance-class carries. Only the first counts toward rule 3's three-of-a-class widening trigger.

---

**ACCRETION-CLASS — counts toward the widening trigger.**

out-of-dimension: the lane's own mandated opening move — an unscoped tier-1 `gloss_index` survey — returns a payload above the tool's output bound as a single line (76,961 characters, observed on PR #399), so the one act §4 makes non-optional for every review is unreadable from a review session without the ad-hoc byte slicing `.claude/skills/review-lane/SKILL.md` rules out of scope for a per-PR review. This is an instrument gap of the same class as kogaki#65 item 3's denial extractor: the fix belongs once in the sweep's own instruments, not once per round in improvised slicing. Recorded, not solved here.

---

**INSTANCE-CLASS — four `carried: register` dispositions from PR #399's round-1 report. Value is the defect named, not the count; these must not be read toward rule 3.**

Each lives in PR #399's own diff text and the preferred remedy is in-review repair this rally; they are carried here so that a merge leaving them open does not evaporate them.

1. `should` — SPEC §4 clause 12 and the new SKILL.md section both ground "(segment sha, 1-based ordinal) is a fact rather than a reading" on "clause 4 makes an earlier segment append-only". §4 clause 4 (`specs/SPEC.md:1025`) carries no such property. The nearest real carrier is a convention comment in `checks/check-review-report.sh:119`/`:691`, and a PR comment is editable.
2. `should` — the new `adjudicates:` section in `.claude/skills/review-lane/SKILL.md` is a conformance copy of clause 12's five sub-rules with **no declared precedence**, nine lines above the `carried:`/`declined:` section that declares one and quotes `conformance-copy-needs-declared-precedence` for why.
3. `nit` — that section states its own unenforcement in its last paragraph, after four paragraphs of present-tense "the gate denies" / "the gate stays red".
4. `nit` — clause 12's PR #255 specimen says "the merge proceeded"; the repair `72d0b9b1` landed twelve minutes before that merge, so the defect never crossed it. The record's own sharper sentence is "nothing in the repository held it".

Full report: https://github.com/tim-nish/kogaki/pull/399#issuecomment-5268401198
