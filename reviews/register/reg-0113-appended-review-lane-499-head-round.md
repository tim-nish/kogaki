---
id: reg-0113
status: pending
observed_at_pr: 499
observed_at_head: 2d91cd1
class:
recorded: 2026-08-17
source_comment: 5316947715
---
Appended by the review lane from PR #499 (head `2d91cd1`, round 1). Three rows; each says its class.

**out-of-dimension: the lane's fixed first move is unreadable through its own instrument.** `.claude/skills/review-lane/SKILL.md` makes an unscoped tier-1 `gloss_index()` survey the review's fixed opening move. On PR #499 it ran and the seam answered, and the result — ~77 KB on a single line — exceeded the tool's read boundary, so nothing in it could be quoted. The only fallback is byte-slicing a saved transcript, which this same file puts out of scope for a per-PR review ("a reviewer that finds itself needing a parser has found a gap in the sweep's own instruments"). The round covered its two dimensions from the repository's own pinned spec text instead. **Accretion-class** — the value is the count of rounds that open ungrounded, not this instance.

**carried (instance-class): `brief/path-review-agent.md` under-declares what path review judges, as of base `4966afb`.** §4.10 (v8, `8bc3b41`, kogaki#492) assigns path review a further conformance judgment — "**Conformance** of the composed path to the adopted register's arc obligations is judged at **path review**" (`specs/spec-draft-pipeline/SPEC.md:667-669`) — which appears in neither the agent contract nor `REVIEW_AREAS`. Correctly outside story 1.74's licence (AC1 is scoped to §§4.4–4.8, and §4.10 states it schedules no implementation), so it is recorded rather than routed at that PR. Lands here because the contract file is now the durable answer to "what does path review judge", and §4.10's obligation is otherwise invisible until its own implementing act. Whether an open issue already owns that act could not be established from the review session (`gh issue list` requires approval; the session is unattended), which is why this is a register row and not an issue number.

**carried (instance-class): SQ1 declared cut in a PR body and re-deferred in the artifact it lands.** PR #499's body says "SQ1's cut: prompt block orchestrated by the sitting"; `brief/path-review-agent.md:4-7` says "Whether it runs as a harness subagent or an inline prompt block is the orchestrating sitting's cut (SQ1)". kogaki#490 carries choice, alternatives or receipt for neither reading. Story 1.75's orchestration inherits an undocumented fork while the PR record reads as though a decision was made. Non-gating and small; recorded because the shape — a story question closed in prose and left open in code — is the one a later sitting cannot see.
