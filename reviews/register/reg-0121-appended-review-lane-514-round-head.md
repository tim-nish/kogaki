---
id: reg-0121
status: pending
observed_at_pr: 514
observed_at_head: 884346c5c43e1cecdb72ff2110f6d00fbaf7df03
class:
recorded: 2026-08-18
source_comment: 5326175041
---
Appended from the review lane, PR #514 round 1 (head `884346c5c43e1cecdb72ff2110f6d00fbaf7df03`). Four rows, each declaring its class.

**Row 1 — instance-class** (non-gating in-diff carry, kogaki#374/#433 floor; auto-merge armed on #514, so no later round can read it). `checks/registry.json:227` — `brief-entry`'s `efficacy_note` drops its tally but keeps its enumeration, and the enumeration disagrees with the pass line: the note names *"story 1.71's FIELDS-row drop, PR #484 round 1's refusal-path mutations"*, while `checks/check-brief-entry.sh:251-263` names *"story 1.71's three refusal-path mutations … failing (g)-(i)"* and no FIELDS-row drop. Twelve members by the note's attribution against the pass line's ELEVEN. One of the two is wrong and the review did not decide which — #511's body endorses the note's attribution, the pass line contradicts it.

**Row 2 — instance-class.** PR #514's decision act (which admission fields must move with a pass line — detector vs removal) is recorded only in the PR body and commit message; licensing issue #511 carries zero comments. `policy/consultation-map.md:105-113`'s served quote requires choice, alternatives and receipt **on the licensing issue before code embeds it**, and a PR body is authored in the same act as the code. Remedy: one comment on #511.

**Row 3 — accretion-class.** An admission note that replaces a *tally* with an *enumeration of the same set* has narrowed the conformance copy, not dissolved it: it now drifts by membership instead of by count, and `registry-conformance` reads neither. Counting the class of "restatement removed in name, retained in form".

**Row 4 — accretion-class, `out-of-dimension:`** (this is the row kind rule 3's three-of-a-class trigger reads). The lane's own re-route path is ungranted in a spawned session: `~/.claude/tools/story-sync file-issue` requires approval and was refused unattended on PR #514, so an instance-class finding SKILL.md routes to a new issue can only reach this ledger — where it is explicitly declared accretion-class. Rows 1 and 2 above are the consequence.
