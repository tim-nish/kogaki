---
story: 1.41
epic: 1
title: "Story 1.41: receipt-absence mining — a boundary touched with no receipt proposes the missing occasion"
status: ready
umbrella: 222
---

Canonical discussion record: https://github.com/tim-nish/kogaki/issues/222 — on any
conflict between this file and the issue, **the issue wins**.

Governing authority: `policy/consultation-map.md` §*Admission and proposal — the miss
rule binds one and not the other*, and the **not asked** postmortem case added to
§*Miss postmortem*, both ratified this sitting (kogaki#222). The second is this story's
own precondition: without it, a mined proposal has no honest value for the postmortem's
question field and the only conformant options were to invent one or to omit the record.

## Story

As the owner growing the consultation map by its own miss loop,
I want a **boundary touched with no receipt** to propose the occasion that was missing,
so that the map learns from the consultations that never happened — which is the half
its miss loop structurally cannot see, because a consultation that does not happen
produces no receipt to harvest.

## Context — what is unobserved today

The record already exists and is read by a human once, then discarded.

`.claude/skills/review-lane/SKILL.md` §2 (*Consultation-map boundaries touched*) has the
lane decide, per map entry, whether the branch touched that boundary, and report **the
entry, what in the diff touched it, and whether a receipt covers it**. That is the
boundary-vs-receipt record, and it is exactly the row this proposer mines: a touched
entry with no covering receipt.

The mechanical half sees a narrower thing. `checks/check-boundary-receipts.sh` binds
**presence** over two enumerations and states its own limit in its head comment: a
receipt's line one names a pin and never a boundary, so nothing mechanically ties receipt
R to boundary B, and *N matched boundaries are satisfied by one receipt*. Its
attribution half is carrier-less-with-a-reopen-trigger, marked rather than omitted. The
review lane's record is where the per-boundary judgment actually lives, which is why it —
and not the check — is this proposer's input.

## PRECONDITION — the record is PROSE today, and this story is not implementable until it is not

**Named at authoring rather than found at implementation.** `SKILL.md` §2 tells the lane
to *"Report, per touched entry: the entry, what in the diff touched it, and whether a
receipt covers it"* — and that is the whole of the format. It declares no field, no token
and no fixed line shape for those three facts, while the six line classes the lane's
format *does* declare (and that `checks/check-review-report.sh` parses) are
`review-lane report:`, `review-base:`, `review-scope:`, `report-complete:`,
`cannot-determine:` and `finding:` — none of which carries the boundary record.
(`out-of-dimension:` is a findings-section convention from the widening-trigger section,
declared in neither the format block nor the checker, and is deliberately **not** counted
here — PR #245 review round 2.)

So an AC that says "mine the rows" would be **unimplementable**, and unimplementable in
exactly the way **entry 2 of this map** exists to catch — an acceptance criterion whose
defect is invisible at review of its own wording. Writing it that way would commit that
entry's own origin-miss class inside the story that is supposed to grow the map by it.

**The record must gain a declared shape before this story can be built, and that edit is
NOT this story's.** `.claude/skills/review-lane/SKILL.md` and the review sweep are held by
**kogaki#224**. **HAND-OFF to that lane:** give §2's per-entry report a declared line
shape — the entry number, what touched it, and a typed covered/uncovered token — so the
boundary-vs-receipt record becomes a readable enumeration rather than prose. No carve-out
issue is minted here; the hand-off is named and the dependency is stated.

`depends_on: the declared-shape edit to review-lane SKILL.md §2 — handed off at
kogaki#224 comment 5223838963, which is the dependency's observable carrier`

**The carrier is the comment, not the issue's title.** kogaki#224 is titled for a
different property, and naming it by file adjacency alone would be the same defect this
PR's round-1 finding 4 raised about `specs/SPEC.md`'s hand-off. The comment states the
edit, states that #224's own scope does not obviously cover it, and leaves the licence
question to the lane holding the file. **This story stays `status: ready` because the
convention has no value expressing "ready except for an unmet precondition"** — the
precondition is declared at the top of this file and in that comment instead, which is
where an implementer meets it (PR #245 review round 2, finding 5).

## Acceptance criteria

**AC1 — the input is the review lane's boundary-vs-receipt record, read at its DECLARED
shape.** Given a review-lane report whose §2 record carries the declared per-entry line
shape above, when a line records a boundary **touched** with **no receipt covering it**,
then a candidate map entry is proposed for the occasion that line names. Lines recording a
covered boundary propose nothing.

**AC1a — an undeclared or unparseable record is CANNOT-DETERMINE, never zero proposals.**
Given a report whose §2 record does not carry the declared shape (a report predating the
hand-off, or a lane run that emitted prose),
when the proposer reads it,
then it reports that it could not establish the record and proposes nothing **while saying
so** — because "no uncovered boundaries" and "I could not read the record" are the same
output otherwise, and the silent reading is the confident wrong one. This criterion is
what keeps the story honest against reports the hand-off has not reached.

**AC2 — a row that fires an EXISTING entry proposes nothing new.** The signal this story
mines is a *missing occasion*. Where the uncovered row names an entry the map already
carries, the finding is an undischarged obligation on that PR — the review lane's own
property — and not a proposal to grow the map. Emitting one would grow the map by
duplicate.

**AC3 — PROPOSAL-ONLY: `policy/consultation-map.md` is not written.** As story 1.40 AC3,
and for the same reason: a proposer that admitted its own findings is the second
authority Invariant 2 refuses
(`consulted: product-lab@dec0d568dd8fc0b2df1185eac10dc1a10600f299 topics/knowledge-architecture.md:69`).

**AC4 — the postmortem is filled as `not asked`, and no question is invented.** Per the
fourth disclosure case ratified this sitting, the proposal says **not asked — derived
from `<the review-lane record and its PR>`** and gives **no** question. A proposer that
composes a plausible question here commits the conformance-copy defect the pinned-quote
rule refuses, moved into a new field — and deletes the only finding the row carried.

**AC5 — the proposal carries its derivation source.** The review-lane row, its report,
and the PR it was authored against travel with the proposal, so a machine-composed
postmortem is legible as such at the admission gate.

**AC6 — it never reads gateway state.** The input is the lane's own report. Reading
gateway internals is mapped boundary **entry 2** of this very file, and a proposer built
to grow that file may not violate it.

**AC7 — fixtures discriminate, both directions.** Mutation evidence per `specs/SPEC.md`
§4: an uncovered-boundary line naming no existing entry **produces** a proposal; a covered
line, and an uncovered line naming an existing entry (AC2), **produce none**; and an
undeclared-shape record **produces the cannot-determine report** (AC1a) rather than
silence.

## Out of scope — declared

- **Admission**, and any write to `policy/consultation-map.md`.
- **The review-lane SKILL.md edit itself** — handed off to kogaki#224's lane above, and a
  precondition of this story rather than a part of it.
- **Widening `check-boundary-receipts.sh`'s attribution half.** That check's carrier-less
  per-boundary attribution has its own declared reopen trigger and its honest fix is a
  receipt grammar change owned by the receipt spec. This story consumes the review lane's
  judgment instead and changes neither.
- **Wholesale corpus derivation** — struck; the one-time seeding batch is not a proposer.
- **Miss harvesting** — story 1.40.

## Deferred slot — FILLED before this story is implemented

`deferred-slot: proposer-siting` **is FILLED** (owner decision 2026-08-08, kogaki#222).
**This proposer is sited in `tools/`**, beside `tools/review-sweep.sh` — it stays in this
repository and does not travel with the client kit.

The axis is **portability**. This proposer's input is
`.claude/skills/review-lane/SKILL.md` §2's boundary-vs-receipt record — an artifact **this**
repository authors and whose shape this repository owns — so it cannot run in another
kit-installing consumer, and a copy of it inside the kit would be a component whose input
does not exist at the other end. Story 1.40's proposer reads only the hub's receipt
grammar and is sited in `policy/kit/bin/` for the same reason read the other way.

The fill's record — choice, both declined alternatives with their grounds, the served
siting rule quoted at its pin, and the consultation receipt — is
`policy/consultation-map.md` §*Admission and proposal* → *`deferred-slot: proposer-siting`
is FILLED*. On any conflict, that record and kogaki#222 win over this paragraph.

**The siting does not discharge this story's precondition.** The two are independent: the
slot's fill says where the file goes, the precondition below says whether there is a record
to read.

## Remaining blocker — STILL OPEN at this filing (2026-08-08)

**This story's readiness is conditioned on the undischarged hand-off above, and it is
recorded here plainly rather than left to be discovered at pickup.** The declared line
shape for `.claude/skills/review-lane/SKILL.md` §2's boundary-vs-receipt record, handed off
at kogaki#224 comment 5223838963, **has not been delivered.** #224's lane merged `15030a4`
— §4 clause 8, the `done` boundary reporting undischarged non-gating findings — which is a
**different** property and nowhere gives §2's record a shape. The merge therefore evidences
that the lane ran, and nothing about this obligation's standing.

Consequences, stated so no sitting has to re-derive them:

- **AC1 is not implementable today.** Its input does not exist in a readable form, which is
  the whole of the PRECONDITION section above.
- **AC1a is implementable today, and against every report that exists**, since a record with
  no declared shape is exactly the cannot-determine case it names. It is not a substitute
  for AC1.
- **`status: ready` is unchanged and is not a claim that the precondition is met** — the
  convention has no value between `ready` and `published` in which "ready except for an
  unmet precondition" could be said (PR #245 review round 2, finding 5). This section is
  where that is said instead.
- **Nothing here is fixed by this sitting.** `.claude/skills/review-lane/SKILL.md` is held
  by the kogaki#191 lane (PR #249, open); the edit stays a hand-off and no carve-out issue
  is minted for it.

Story 1.40 carries no equivalent block and is publishable on this fill alone.

## Predicted file set

A new proposer entry point under **`tools/`**, plus its fixtures. No edit to
`policy/consultation-map.md`, `checks/check-boundary-receipts.sh`, or
`.claude/skills/review-lane/SKILL.md`.
