---
story: 1.32
epic: 1
title: "Story 1.32: Name the Terrain surfaces in owner-facing register — the tag screen and the co-tag screen"
status: published
depends_on: []
issue: 162
---

Issue: https://github.com/tim-nish/kogaki/issues/162. On conflict, the issue
wins.

As the Kogaki repository owner, I want Terrain's surfaces named in a register
I can parse — "the tag screen" and "the co-tag screen" — so that no reply,
prompt or relayed line ever hands me an internal label like "screen-1" that
carries no more information to me than a redacted placeholder.

## Context

The 2026-08-07 dogfood run asked the owner *"Before the co-tag screen, do you
want a screen-1 pass?"*. The issue's own `searched:` declaration locates the
mechanism: the numbered names are the skill's and the spec's **own surface
names**, so the leak reproduces from the source vocabulary rather than from a
one-off slip in one reply. Renaming the source is what makes the leak
unreachable; a rule forbidding the label on the owner surface would leave the
source intact and police the output.

The sites are located in the shipped text:

- `.claude/skills/terrain/SKILL.md:29` — `2. **Navigate — screen 1** — …`
- `.claude/skills/terrain/SKILL.md:32-33` — "Screen 1's axis is the served tag
  vocabulary."
- `.claude/skills/terrain/SKILL.md:34` — `3. **Navigate — screen 2, the co-tag
  step** — …`
- `specs/spec-terrain/SPEC.md:1066` — "the decision shards have addresses and
  screen 1 discloses nothing about them" (inside a **quoted served refusal**;
  see AC5).

## Scope — the vocabulary half only

**The fork half of issue #162 is NOT in this story.** The issue's second
proposed fix — "a tag named by the owner lands DIRECTLY at the co-tag step …
No question mediates it" — is entailed by the question-affordance allowlist
decided across kogaki#161 / #164 / #166 as one coupled spec group. Writing it
as an acceptance criterion here would duplicate that group's decision or be
re-cut after it. It is carried as a story question below.

Note that `.claude/skills/terrain/SKILL.md:37` already carries the
destination half of that contract — *"**This is where a tag selection lands,
not a second `view --tag`.**"* — so what the group decides is the no-question
rule, not the destination.

## Acceptance criteria

**AC1 — the skill's flow steps carry owner-facing names.**
Given `.claude/skills/terrain/SKILL.md` steps 2 and 3, currently headed
"Navigate — screen 1" (`:29`) and "Navigate — screen 2, the co-tag step"
(`:34`), when the step headings are rewritten, then they read "**Navigate —
the tag screen**" and "**Navigate — the co-tag screen**", and no numbered
stage label remains in either heading.

**AC2 — no numbered stage label survives anywhere the owner reads.**
Given the skill's prose body, when `grep -niE 'screen[ -][12]' .claude/skills/terrain/SKILL.md`
is run, then it returns no line that the harness would relay to or speak in
front of the owner. A residual match is admissible only where AC5's exemption
applies, and each such line is named in the PR body.

**The detector is case-INSENSITIVE, and that is load-bearing rather than
incidental.** The case-sensitive form this criterion first carried
(`grep -nE`) returned only `:29` and `:34` at head and was blind to `:32`
("Screen 1's axis …") — a site this story's own Context lists. A criterion
whose detector cannot see a site its story names does not fail the story; it
passes it wrongly, which is the worse of the two.

**AC3 — the replacement names are used consistently, not variably.**
Given the two surfaces, when either is referred to in owner-facing prose,
then it is called "the tag screen" or "the co-tag screen" — these two strings
and no synonyms ("the first screen", "the tag view", "the narrowing screen"),
because a second coinage reintroduces the parse problem the rename fixes.

**AC4 — the spec's internal section references are untouched.**
Given `specs/spec-terrain/SPEC.md`'s own section numbering (§6, §9, §6.1,
§6.2), when this story lands, then that numbering is unchanged: the issue
states that "if internal section references need them, they stay internal",
and renumbering a spec is not this story's change.

**AC5 — the served quotation at `specs/spec-terrain/SPEC.md:1066` is NOT
edited.**
Given that line — "the decision shards have addresses and screen 1 discloses
nothing about them" — when the rename is applied, then that occurrence stays
verbatim, because it sits inside a block-quoted served hub refusal at
`specs/spec-terrain/SPEC.md:1064-1070`, whose own pin line names
`product-lab@f918c515… topics/articles.md:94`. §3's quote-at-the-pin
discipline governs every served rendering Terrain emits, and silently editing
a quoted served line to match a local vocabulary decision would serve an
unratified rendering of ratified text. Where the surrounding Kogaki prose
introduces or glosses that quote, the Kogaki prose may be renamed; the quote
may not.

**This is a THIRD exemption category, and #162 enumerates two** — owner-facing
prose (renamed) and internal section references (left alone). A category the
issue does not authorize may not be decided in the implementation alone, so it
is recorded on the issue through the typed path
(`story-sync policy-verdict 162`): kogaki#162 comment 5212574990, carrying the
served ground and the readings dismissed. AC5 stands on that record, not on
this file.

*(The pin above is quoted here in mention form — a shortened sha and no field
name — so that a story file restating a spec's pin cannot be harvested as one.
The pin itself, and the one-line drift found while re-resolving it, belong to
`specs/spec-terrain/SPEC.md`; this story edits no line of that file. See the
recorded verdict.)*

## Story questions — not criteria

- **Does the rename extend to the spec's own section titles, or only to
  relayed prose?** Cannot-determine from the shipped text: `specs/spec-terrain/SPEC.md`
  §6 and §9 use the numbered names as section vocabulary, and whether a spec
  heading counts as "everything the owner reads" is a judgment the issue does
  not settle. AC4 takes the narrow reading; widening it is a separate decision.
- **The no-question rule at the tag→co-tag transition** is decided in the
  coupled spec group kogaki#161 / #164 / #166 and is not decided here.

## Files this story is predicted to touch

`.claude/skills/terrain/SKILL.md` (steps 2–3 and the flow-rule preamble), and
Kogaki-authored prose in `specs/spec-terrain/SPEC.md` that introduces the
surface names. **This overlaps the files the kogaki#161 / #164 / #166 stories
will touch.** That overlap is a scheduling consideration for whoever sequences
the branches — it is not a dependency, and this story's criteria are decided
by nothing in that group.

**Files actually touched: `.claude/skills/terrain/SKILL.md` and this file.
`specs/spec-terrain/SPEC.md` is NOT touched, and no criterion requires it.**
The prediction above assumed the spec carries Kogaki prose that *introduces*
the surface names to the owner. It does not: every numbered occurrence there is
either the spec's own section vocabulary (`:12`, `:146`, `:887`, `:897`), which
AC4's narrow reading leaves alone and the first story question defers, or the
served quotation AC5 protects (`:1066`). Widening the rename to spec prose is
the separate decision that question names, not this story's change — and the
file is concurrently held by the kogaki#167 / #168 sitting.

Two internal, non-owner-facing sites are likewise left alone and named here so
a later reader does not read them as misses: `checks/registry.json:83` (a
check-admission `contract` record, quoting §9's own section vocabulary) and
`terrain/terrain.mjs:312,397` (source comments). AC2 is scoped to the skill's
prose body; AC3 to owner-facing prose. Neither is either.
