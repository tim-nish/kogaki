# Fixture: a served quotation carrying no pin

Constructs kogaki#583 instance 3: a served-surface pointer with no
`<repo>@<sha>` on its own line and none in the adjacent block, in a corpus
where sibling quotations carry one. Report-only by admission-record decision;
the resolver must still name it.

"a served line quoted with nothing that says which revision of the surface it was read at"
topics/claude-code-ops.md:15
