# Moves

Regenerated whole from `moves/` at each ingestion run (SPEC-draft-pipeline
§6.9.1a). Every column is read off a file; none is composed. Nothing reads
this file to decide anything.

**The 22 Moves the eight-field schema produced were retired in full
(kogaki#1175, 2026-09-23)** — they were extracted from the same sections of
the reference work the Corpus analyzes, so each is superseded by an Analysis
of the same text under the rebuilt schema (`specs/spec-draft-pipeline/SPEC.md`
§4.2), and their existence was not a reason to keep them. Git history is the
retention; no file under `moves/` carries a schema-date marker because none
predates the schema anymore.

**One record below** — `enter_a_hard_subject_through_the_readers_own_world`
— admitted under the rebuilt schema as this Issue's re-extraction
verification (§4.2's acceptance item 2): the opening Passage of the
geopolitics primer (`passages/FORMAT.md`'s worked example), re-extracted
through the rewritten `specs/move-extraction-contract.md` and admitted
through `tools/move_ingest.py` with zero refusals.

**What does not appear here yet.** The Corpus-to-Moves conversion this
Issue's thread also calls for — every Analysis in the Corpus becoming one
Move in the rebuilt schema — did not run in this worktree: the Corpus lives
at `/home/tomoya/work/kogaki/corpus/` in the main checkout, outside this
worktree's licensed and reachable paths. The next ingestion run that has the
Corpus in reach populates the rest of this index; nothing here composes a
row for a Move that is not yet a file.

| id | technique |
| --- | --- |
| enter_a_hard_subject_through_the_readers_own_world | Before the first claim that needs the subject, give the reader three things that need none: something they have seen, a map of the kinds of text on the subject, and an admission by the author that costs something. Then name the subject's central question in the reader's voice and answer it in one sentence. |
